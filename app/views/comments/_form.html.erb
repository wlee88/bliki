<%= form_for([@box, @box.comments.build]) do |f| %>
		<div class="field">
			<%= f.label :body %><br />
			<%= f.text_area :body %><br />
		</div>
		<div class="actions">
			<%= f.submit %>
		</div>
<% end %>