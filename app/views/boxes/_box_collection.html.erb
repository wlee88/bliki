<span id="title_text_large">My Items</span>
<div id="paginate"><%= will_paginate @boxes%></div>
<br />
<br />
<hr />
<br />
<div class ="mybox_me">
	<%= link_to "Add", new_box_path%>
	<%= link_to image_tag ("/images/stock/Add.png"),new_box_path %>
	<span class="box_options">+</span>
	
</div>	
<% @boxes.each do |box| %>
<% if !box.desc.nil?%>

	<div id="<%= box.id.to_s %>" class="mybox_me" >
			<span class="box_options"><%= link_to "Delete", box_path(box), :method => "delete"%></span>
				
				<%= link_to image_tag (box.image.url), edit_box_path(box), :title => "Description: " + box.desc%>
				<span class="box_options"><%= link_to "Edit", edit_box_path(box) %></span>
		</div>
	
<% end %>
<% end %>
